#pragma once
// this is lib.h, it's the include file for lib.cpp
// it should include no code at all 
// (except in one case that will be covered in the end of semester)
// but only declarations like function signatures

int add2(int a, int b);